version: "3.8"

services:
  backend:
    build: .
    container_name: musicplanet-backend
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://aws-1-eu-central-2.pooler.supabase.com:5432/postgres
      SPRING_DATASOURCE_USERNAME: postgres.phibuizpsogapbgfxcrb
      SPRING_DATASOURCE_PASSWORD: 339W4Ra2EvRJKaXr
      JWT_SECRET: mySecretKeyForJWTtokenGenerationMustBeLongEnough256bits
      SPRING_PROFILES_ACTIVE: prod
    ports:
      - "8081:8081"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_URL: http://localhost:8081/api
    container_name: musicplanet-frontend
    depends_on:
      backend:
        condition: service_healthy
    ports:
      - "80:80"

